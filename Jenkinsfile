pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh 'echo "Hello from Jenkins"'
                sh 'ls -la'
                sh 'pwd'
            }
        }
    }
}
